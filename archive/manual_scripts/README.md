# Archived Manual Scripts

These scripts are older local command-line utilities for running workflow checks
directly from Python.

They are not part of the active app surface. The active backend entrypoint is
the FastAPI app in `backend/api`, and the active frontend is `frontend/`.

Prefer API routes or backend services for new work. Keep these scripts only for
manual debugging, demos, or local operator checks.




