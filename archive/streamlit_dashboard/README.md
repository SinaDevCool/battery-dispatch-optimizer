# Archived Streamlit Dashboard

This folder contains the original Streamlit dashboard used for internal
prototyping and manual workflow checks.

The active commercial frontend is the Next.js application in `frontend/`.
Keep new product UI work there unless a change is specifically for this archived
dashboard.

Run locally with:

```bash
python -m streamlit run archive/streamlit_dashboard/app.py
```





